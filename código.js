document.getElementById("Input_Menú").addEventListener("submit", function (event) {
    event.preventDefault(); // Evita que el formulario se envíe por defecto

    // Aquí puedes realizar validaciones u otras acciones

    // Luego, redirige al usuario a la página de destino
    window.location.href = "Menú.html";


});
